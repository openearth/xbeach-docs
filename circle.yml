general:
  build_dir: docs

compile:
  override:
    - make html &> build.log
            
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/nose
    - nosetests --with-xunit --xunit-file=$CIRCLE_TEST_REPORTS/nose/tests.xml